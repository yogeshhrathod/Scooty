# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
.pnpm-store

# Build Output
dist
dist-ssr
dist_electron
release
build

# Environment Variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# System Files
Thumbs.db
desktop.ini

# Capacitor
# Web assets are built into the native projects, so generally we might want to ignore them if we only care about source
# But usually standard Capacitor ignore is minimal.
# However, we should ignore native build artifacts:
android/app/build
android/.gradle
ios/App/App/public
ios/App/App/build

# Testing
coverage

# Temporary
tmp
temp